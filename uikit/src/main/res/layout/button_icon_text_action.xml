<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rlRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/selectableItemBackground"
    android:gravity="center">

    <ImageView
        android:id="@+id/ivIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:src="@drawable/ic_add" />

    <RelativeLayout
        android:id="@+id/rlTextContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignWithParentIfMissing="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="5dp"
        android:layout_marginStart="5dp"
        android:layout_toEndOf="@+id/ivIcon"
        android:layout_toLeftOf="@+id/btnEdit"
        android:layout_toRightOf="@+id/ivIcon"
        android:layout_toStartOf="@+id/btnEdit">

        <TextView
            android:id="@+id/tvButtonTitle"
            style="@style/IconTextActionButtonTitle"
            android:layout_width="match_parent"
            android:ellipsize="end"
            android:maxLines="1" />

        <TextView
            android:id="@+id/tvButtonDescription"
            style="@style/IconTextActionButtonDescription"
            android:layout_width="match_parent"
            android:layout_below="@+id/tvButtonTitle"
            android:ellipsize="end"
            android:maxLines="1" />
    </RelativeLayout>

    <ImageButton
        android:id="@+id/btnEdit"
        style="@style/ButtonEdit"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="10dp"
        android:visibility="gone" />

</RelativeLayout>